name: delete-closed-pr-docs

on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch:

jobs:
  delete-closed-pr-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Delete closed PR docs
        uses: KeisukeShima/autoware-github-actions/delete-closed-pr-docs@79-delete-closed-pr-docs-follow-up
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
